Readme
Anthony Elliott
ajellio2
12/15/2014
Graphics

Project 3: Space Invaders 3D

I used WebGL for this.

See most recent version on http://anthonyelliott.me/space-invaders-3d/
  OR
you can start a web server locally and open the index.html file.

If you want to follow the entire logic, start at the end of the file.
Once the HTML is loaded, `webGLStart()` is called which starts everything.

Otherwise, `drawScene()` is called every 15 ms if you want to shortcut.
